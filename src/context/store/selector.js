import { createSelector } from '@reduxjs/toolkit';

// export const dataSelector  = (state)=> state.dataDienNuoc.DienNuoc;

// export const dataRemainingSelector = createSelector({
//   dataSelector,
// })